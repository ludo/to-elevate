!!! XML
!!! Strict
%html{html_attrs('en')}
  %head
    %title
      = catch_content :for_title
      to elevate
    = css_include_tag :to
    = css_include_tag :master
    = css_include_tag :typography
    = js_include_tag :jquery
  %body
    #auth
      .container
        %ul= partial "layout/auth"
        %br.clear
    .container
      #header
        - if logged_in?
          #user.right= current_user.login
          = image_tag gravatar(current_user.email), :class => "right avatar"
        %h1
          to
          = link_to "elevate", url(:root)
      #nav
        = render_menu if logged_in?
        %br.clear
      #main= catch_content :for_layout
      #footer
        %p 
          to
          = link_to "elevate", "http://github.com/ludo/to-elevate/wikis/home", :class => "to elevate"
          is developed by 
          = link_to "cubicphuse", "http://cubicphuse.nl"